def int calculate_sum_and_product(param int x, param int y)|{| let int sum_val;| let int prod_val;| | sum_val = x + y; | prod_val = x * y % (sum_val);|| return prod_val; |};||def string concatenate_if(param string s1, param string s2, param bool condition)|{| let string result;| | if(condition):| {| result = s1; | }| else| {| result = s2;| }| return result; |};||def string errdef(param int a, param int b)|{| return 'ddd'; |};||main {| let int a;| let int b;| let string final_str;| let int result_int;| let bool chec1_true = 1; | let bool chec2_false = 0;| let int c;|| a = 10;| b = 5;| errdef(a, b, c); || print '--- Тест 1: Вызов функции с возращаемым значением int ---';| print 'a+b * b % (a+b):';| print calculate_sum_and_product(a, b); | | print '--- Тест 2: Вызов функции с возращаемым значением string (внутри проверка bool) ---';| print 'Результат должен быть истина:';| print concatenate_if('Истина', 'Ложь', chec1_true);| | print '--- Тест 3: Вызов функции с возращаемым значением string (внутри проверка bool) ---';| print 'Результат должен быть ложь:';| print concatenate_if('Истина', 'Ложь', chec2_false); | | print '--- Тест 4: Проверка if (а == 15) ---';| if(a == 15):| {| print 'Истина';| }| else| {| print 'Ложь'; | }| | print '--- Тест 5: Switch(50) ---';| switch(50) | {| case 50: | {| print 'Успех! это 50';| }| case a: | {| print 'Неверно! это a';| }| case 5: | {| print 'Неверно! это 5';| }| }| | print '--- Тест 6: Попытка вывести тип данных bool (false)---';| print check_true; || return 0;|};